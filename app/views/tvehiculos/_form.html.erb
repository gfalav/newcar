<%= form_for(@tvehiculo) do |f| %>
  <% if @tvehiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tvehiculo.errors.count, "error") %> prohibited this tvehiculo from being saved:</h2>

      <ul>
      <% @tvehiculo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
