<%= form_for(@tturno) do |f| %>
  <% if @tturno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tturno.errors.count, "error") %> prohibited this tturno from being saved:</h2>

      <ul>
      <% @tturno.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :turno %><br />
    <%= f.text_field :turno %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
